apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: ispdfgen
  namespace: teamsykefravr
  labels:
    team: teamsykefravr
spec:
  image: {{ image }}
  port: 8080
  replicas:
    min: 1
    max: 2
    cpuThresholdPercentage: 70
  liveness:
    path: /internal/is_alive
    periodSeconds: 5
    timeout: 5
    failureThreshold: 3
  readiness:
    path: /internal/is_ready
    periodSeconds: 5
    timeout: 5
    failureThreshold: 3
  startup:
    path: /internal/is_ready
    periodSeconds: 5
    timeout: 5
    failureThreshold: 30
  prometheus:
    enabled: true
    path: /internal/metrics
  resources:
    limits:
      memory: 500Mi
    requests:
      cpu: 5m
      memory: 100Mi
  ingresses:
    - "https://ispdfgen.intern.dev.nav.no"
  observability:
    autoInstrumentation:
      enabled: true
      runtime: java
    logging:
      destinations:
        - id: loki
        - id: elastic
  accessPolicy:
    inbound:
      rules:
        - application: isaktivitetskrav
        - application: isfrisktilarbeid
        - application: isarbeidsuforhet
        - application: isdialogmote
        - application: isbehandlerdialog
        - application: ismanglendemedvirkning
        - application: isoppfolgingsplan
        - application: isutenlandsopphold
        - application: padm2
  env:
    - name: DISABLE_PDF_GET
      value: "false"
    - name: DISABLE_PDFA_VALIDATION
      value: "false"